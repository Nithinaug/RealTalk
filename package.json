{
  "scripts": {
    "build": "echo \"const CONFIG = { SUPABASE_URL: '$SUPABASE_URL', SUPABASE_ANON_KEY: '$SUPABASE_ANON_KEY', BACKEND_URL: '$BACKEND_URL' };\" > web/config.js"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
